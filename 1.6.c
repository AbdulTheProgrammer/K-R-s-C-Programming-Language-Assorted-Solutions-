#include <stdio.h>
/* copy input to output; 2nd version */ 

int main() { 
	int c; 
	printf("This is EOF %i", EOF); 
	
	while((c = getchar()) != EOF) 
	{
		printf("This is the value of this expression %i", c = getchar() != EOF);	
		putchar(c); 
	}
	return 0;
}
